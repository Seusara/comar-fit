# Comar-Fit branding

`masters/` contains the original, lossless brand artwork. `play-store/` contains publication-only artwork and is not imported by the application bundle. Optimized runtime assets live in `src/assets/branding/`.
